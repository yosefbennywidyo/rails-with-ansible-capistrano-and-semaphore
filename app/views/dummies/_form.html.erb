<%= form_with(model: dummy) do |form| %>
  <% if dummy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dummy.errors.count, "error") %> prohibited this dummy from being saved:</h2>

      <ul>
        <% dummy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
